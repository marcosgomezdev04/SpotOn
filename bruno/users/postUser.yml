info:
  name: postUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/user/post
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "UsuarioGenerico1",
          "email": "Generico1@gmail.com",
          "password": "1234567"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
