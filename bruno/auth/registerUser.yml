info:
  name: registerUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/register
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "Test404",
          "email": "Testo404@gmail.com",
          "password": "Test404"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
