info:
  name: loginUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/login
  body:
    type: json
    data: |-
      {
          "email": "Testo404@gmail.com",
          "password": "Test404"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
