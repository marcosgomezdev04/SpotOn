info:
  name: deleteSchedule
  type: http

http:
  method: DELETE
  url: http://localhost:3000/schedule/delete/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
