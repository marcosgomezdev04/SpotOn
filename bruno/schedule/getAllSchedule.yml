info:
  name: getAllSchedule
  type: http

http:
  method: GET
  url: http://localhost:3000/schedule/getAll
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
